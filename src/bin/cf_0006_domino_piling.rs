use std::io;
use rust::utils::input_two_integers::input_two_integers;
const EXTRA_LOGS: bool = true;

fn main() {
    
    if EXTRA_LOGS {println!("Introduce un número entero (será el número de palabras que introducirás después):");}

    // m = ancho, n = alto, del tablero
    let ( m, n ) = input_two_integers(Some(EXTRA_LOGS));




}